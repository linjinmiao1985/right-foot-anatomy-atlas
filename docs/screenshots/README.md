# Teaching screenshots

Generated by `npm run screenshots` (`scripts/screenshot-pipeline.mjs`).

**Honesty**: teaching-grade QA pack for expert review — **not** a finished-product gallery.
Soft-tissue layers remain incomplete; nerve / many soft meshes are BY-SA isolate.

Pack (Day 4de): **12 shots** — layer/clip set (`01`–`05`) + multi-view camera presets
(bone dorsal / plantar / medial + all-layers lateral, `06`–`09`) + **Week 2 teaching modes**
(`10`–`12`: ghost opacity Day 4bm / explode Day 4bn / ghost+explode combined).

Pipeline turns sagittal clip **off** before multi-view shots so presets are not cropped.
Ghost and explode shots use `G` and `E` keyboard presets (teaching prefs persist in localStorage).

See `manifest.json` for shot list and generation timestamp.

Also linked from `docs/journal-figure-captions.md` (Figures 1–3: bones overview, layers/ghost, explode) and root `README.md` (Teaching QA screenshots) for expert review — still **not** a product gallery.
